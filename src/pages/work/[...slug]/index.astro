---
import WorkLayout from "@/layouts/WorkLayout.astro";
import works from "@/data/works.json";

type WorkData = {
  htmlContent?: string;
  [key: string]: any;
};

const param = Astro.params;

// const data = works.find((work) => {
//   const [lang, ...slug] = work.slug.split("/");
//   return lang === Astro.currentLocale && slug.join("/") === param.slug;
// });

const data = works[0];

---

<WorkLayout data={data}>
  <div set:html={data?.data?.htmlContent} />
</WorkLayout>
